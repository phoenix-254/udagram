apiVersion: v1

kind: Service

metadata:
  name: udagram-user-service
  labels: 
    run: udagram-user-service

spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    run: udagram-user-service